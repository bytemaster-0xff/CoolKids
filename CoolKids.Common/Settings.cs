namespace CoolKids.Common
{
	public class Settings
    {
        /*
        public static String AzureAccount = "tobeadded";
        public static String AzureStorageKey = "tobeaddded";
        public static String AzureTableStorageURL = String.Format("http://{0}.table.core.windows.net/", AzureAccount);

        public static List<String> EOFChars = new List<String>{ "CR", "LF", "EOF" };
		
		public static CloudStorageAccount _amsStorageAccount;
        public static CloudStorageAccount AMSSorageAccount
        {
            get
            {
                if (_amsStorageAccount == null)
                {
                    var creds = new StorageCredentials(AzureAccount, AzureStorageKey);
                    _amsStorageAccount = new CloudStorageAccount(creds, true);
                }

                return _amsStorageAccount;
            }
        }
		*/
    }
}